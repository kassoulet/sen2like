name: hyperspectral2chime
channels:
  - conda-forge
dependencies:
  - python=3.10
  - gdal=3.6.3
  - numpy=1.24
  - scipy=1.10
  # Optional, only for mission-specific HDF5/NetCDF readers (e.g. PRISMA):
  - h5py=3.8.0
  # Dev tooling:
  - pytest
  - black
  - isort
